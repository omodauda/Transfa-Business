import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  content: {
    alignItems: 'center'
  },
  title: {
    marginTop: 50,
    marginBottom: 32
  },
  centeredText: {
    textAlign: 'center'
  }
})

export default styles;