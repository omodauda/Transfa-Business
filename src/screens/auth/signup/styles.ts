import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  centeredView: {
    alignItems: 'center',
  },
  step: {
    marginBottom: 22
  },
  description: {
    textAlign: 'center',
    marginTop: 29
  },
  terms: {
    textAlign: 'center',
    marginTop: 16
  }
})

export default styles;